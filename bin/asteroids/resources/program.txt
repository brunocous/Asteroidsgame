double x;
double y;
double r;
entity nearestAsteroid;
entity a;
double distance;
double distanceToNearestAsteroid;
double xpos;
double ypox;
double angle;
double tangent;
x := getx 0.2;
y := gety self;
r := getradius self;
nearestAsteroid := null;
foreach(asteroid, a) do {
ax := getx a;
ay := gety a;
ar := getradius a;
distance := sqrt((((x - ax) * (x - ax)) + ((y - ay) * (y - ay))));
if(nearestAsteroid == null) then {
nearestAsteroid := a;
		distanceToNearestAsteroid := distance;
	} else {
		if(distance < distanceToNearestAsteroid) then {
			nearestAsteroid := a;
			distanceToNearestAsteroid := distance;
			}
		}
}
xpos := (getx nearestAsteroid) - (getx self);
ypos := (gety nearestAsteroid) - (gety self);
tangent := ypos / xpos;
angle :=  tangent - (((tangent * tangent) * tangent) / 3) + (((((tangent * tangent) * tangent) * tangent) * tangent ) / 5) - getdir;
turn angle;
fire;
thrust;
thrust_off;